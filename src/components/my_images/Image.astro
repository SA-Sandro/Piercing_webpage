---
interface Props {
  src: string;
  width?: number;
  height?: number;
  class?: string;
  alt: string;
}

const { src, width, height, class: className, alt } = Astro.props;
---

<img src={src} width={width} height={height} class={className} alt={alt} />
