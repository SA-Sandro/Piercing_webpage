---
interface Props {
  src: string;
  width: number;
  height: number;
  class: string;
  alt: string;
}

const { src, width, height, alt } = Astro.props;
---
<img
  src={src}
  width={width}
  height={height}
  :class={class} 
  alt={alt}
/>