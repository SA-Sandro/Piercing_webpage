---
interface Props {
  src: string;
  width: number | null;
  height: number | null;
  class: string | null;
  alt: string;
}

const { src, width, height, alt } = Astro.props;
---
<img
  src={src}
  width={width}
  height={height}
  :class={class} 
  alt={alt}
/>